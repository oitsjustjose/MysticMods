package MysticOres;

/*
import ic2.api.Ic2Recipes;

import java.util.ArrayList;

import cpw.mods.fml.common.Loader;
import cpw.mods.fml.common.registry.GameRegistry;

import net.minecraft.item.ItemStack;
import net.minecraftforge.oredict.OreDictionary;

public class IC2Handler
{

	public static void adamantineRecipe()
	{
		ItemStack tOutput = new ItemStack(MysticOres.AdamantineDust, 2, 0);
		ArrayList<ItemStack> tList = OreDictionary.getOres("oreAdamantine");
		for (int i = 0; i < tList.size(); i++) {
		    ItemStack tStack = tList.get(i);
		    tStack = tStack.copy();
		    tStack.stackSize = 1;
		    Ic2Recipes.addMaceratorRecipe(tStack, tOutput);
		}
	}
	
	public static void mithrilRecipe()
	{
		ItemStack tOutput = new ItemStack(MysticOres.MithrilDust, 2, 0);
		ArrayList<ItemStack> tList = OreDictionary.getOres("oreMithril");
		for (int i = 0; i < tList.size(); i++) {
		    ItemStack tStack = tList.get(i);
		    tStack = tStack.copy();
		    tStack.stackSize = 1;
		    Ic2Recipes.addMaceratorRecipe(tStack, tOutput);
		}
	}
	
	public static void verditeRecipe()
	{
		ItemStack tOutput = new ItemStack(MysticOres.VerditeDust, 2, 0);
		ArrayList<ItemStack> tList = OreDictionary.getOres("oreVerdite");
		for (int i = 0; i < tList.size(); i++) {
		    ItemStack tStack = tList.get(i);
		    tStack = tStack.copy();
		    tStack.stackSize = 1;
		    Ic2Recipes.addMaceratorRecipe(tStack, tOutput);
		}
	}
	
}
*/